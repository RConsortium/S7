# Evolution compat lab results

Generated by `Rscript tools/evolution/run.R`. Do not edit by hand.

* Date: 2026-07-30
* S7 version: 0.2.2.9000
* R version: 4.5.3
* Mode: install + test + R CMD check

Stages: evoB is written against evoA 1.0.0. "stale" = evoA upgraded to
2.0.0 but evoB not rebuilt (what a user sees after upgrading only evoA);
"v2" = evoB rebuilt against evoA 2.0.0 (what R CMD check sees).

## gen-add-arg

A adds an optional argument to a generic that has `...`; B's method lacks it. Expect warnings when evoB is (re)installed and loaded against 2.0.0; calls still work.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | OK |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | 1 NOTE |

`evoB R CMD check (v2)`:

```
Warning: gen(<evoB::BClass>) doesn't have argument `verbose`

```

## gen-add-arg-fixed

As gen-add-arg, but B's method already has the argument A is about to add (the recommended transition). Expect everything OK against both versions, since methods may have arguments the generic lacks.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | OK |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | OK |

## gen-add-arg-nodots

A adds an argument to a generic *without* `...`, where method formals must match exactly. Expect evoB to fail to install against 2.0.0, with no version of B that works for both.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | ERROR |
| evoB install (v2) | OK |
| evoB test (v2) | ERROR |
| evoB R CMD check (v2) | 1 ERROR |

`evoB test (v2, stale)`:

```
Error in `method(gen, evoB::BClass)`(x = <object>, verbose = FALSE) : 
  unused argument (verbose = FALSE)
```

`evoB test (v2)`:

```
Error in `method(gen, evoB::BClass)`(x = <object>, verbose = FALSE) : 
  unused argument (verbose = FALSE)
```

`evoB R CMD check (v2)`:

```
* checking whether the package can be loaded ... ERROR
Loading this package had a fatal error status code 1
Error: package or namespace load failed for ‘evoB’:
 .onLoad failed in loadNamespace() for 'evoB', details:
Status: 1 ERROR
See
```

## gen-remove-arg

A removes an optional argument; B's method still has it. Expect this to be silent (methods may have extra arguments) so B can drop the argument at leisure.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | OK |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | OK |

## gen-change-default

A changes the default of a non-dispatch argument; B's method uses the old default. Expect a default-mismatch warning at install/load against 2.0.0 (and B matching 2.0.0 would warn against 1.0.0).

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | OK |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | 1 NOTE |

`evoB R CMD check (v2)`:

```
Warning: In gen(<evoB::BClass>), default value of `drop` is not the same as the generic
- Generic: TRUE
```

## gen-rename

A renames a generic with no alias. Expect evoB to fail to install against 2.0.0 (`evoA::gen1` no longer exists).

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | ERROR |
| evoB install (v2) | ERROR |
| evoB test (v2) | ERROR |
| evoB R CMD check (v2) | 1 ERROR |

`evoB test (v2, stale)`:

```
Error: package or namespace load failed for ‘evoB’:
 object ‘gen1’ is not exported by 'namespace:evoA'
```

`evoB install (v2)`:

```
Error: object ‘gen1’ is not exported by 'namespace:evoA'
Execution halted
ERROR: lazy loading failed for package ‘evoB’
* removing ‘/private<lab>/gen-rename/lib/evoB’
```

`evoB test (v2)`:

```
Error: package or namespace load failed for ‘evoB’:
 object ‘gen1’ is not exported by 'namespace:evoA'
```

`evoB R CMD check (v2)`:

```
* checking whether package ‘evoB’ can be installed ... ERROR
Installation failed.
Status: 1 ERROR
See
```

## gen-rename-alias

A renames a generic but keeps the old name as an exported alias. Expect B (which still registers on the old name) to keep working, and methods to be reachable through both names.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | OK |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | OK |

## gen-rename-wrapper

A renames a generic and turns the old name into a deprecating wrapper function. Callers of the old name keep working (with a warning), but does downstream method registration on the old name still work, given that the wrapper is not a generic?

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | ERROR |
| evoB install (v2) | ERROR |
| evoB test (v2) | ERROR |
| evoB R CMD check (v2) | 1 ERROR |

`evoB test (v2, stale)`:

```
Error: package or namespace load failed for ‘evoB’:
 .onLoad failed in loadNamespace() for 'evoB', details:
```

`evoB install (v2)`:

```
Error in `method<-`(`*tmp*`, BClass, value = function (x, ...)  : 
  `generic` is a function, but not an S3 generic function:
Error: unable to load R code in package ‘evoB’
Execution halted
ERROR: lazy loading failed for package ‘evoB’
* removing ‘/private<lab>/gen-rename-wrapper/lib/evoB’
```

`evoB test (v2)`:

```
Error: package or namespace load failed for ‘evoB’:
 .onLoad failed in loadNamespace() for 'evoB', details:
```

`evoB R CMD check (v2)`:

```
* checking whether package ‘evoB’ can be installed ... ERROR
Installation failed.
Status: 1 ERROR
See
```

## gen-add-dispatch-arg

A converts a single-dispatch generic to double dispatch. Expect evoB to fail to install against 2.0.0: the method signature and formals no longer conform.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | ERROR |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | 1 ERROR |

`evoB install (v2)`:

```
Error in `method<-`(`*tmp*`, BClass, value = function (x, ...)  : 
  `signature` must be a list for multidispatch generics.
Error: unable to load R code in package ‘evoB’
Execution halted
ERROR: lazy loading failed for package ‘evoB’
* removing ‘/private<lab>/gen-add-dispatch-arg/lib/evoB’
```

`evoB R CMD check (v2)`:

```
* checking whether package ‘evoB’ can be installed ... ERROR
Installation failed.
Status: 1 ERROR
See
```

## gen-move-package

A moves a generic to a lower-level package (evoACore) and re-exports it. Expect B (which imports the generic from evoA) to keep working: registration follows the generic object to its home package.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoACore 2.0.0: install | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | OK |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | OK |

## gen-move-package-copy

As gen-move-package, but evoA re-exports with a binding copy (`gen <- evoACore::gen`) instead of a NAMESPACE re-export. Expect dispatch to fail: the copy serialized into evoA has its own methods table, separate from the one B registers into.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoACore 2.0.0: install | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | OK |
| evoB test (v2) | ERROR |
| evoB R CMD check (v2) | 1 ERROR |

`evoB test (v2)`:

```
Error: Can't find method for `gen(<evoB::BClass>)`.
Execution halted
```

`evoB R CMD check (v2)`:

```
 ERROR
Running the tests in ‘tests/smoke.R’ failed.
  Error: Can't find method for `gen(<evoB::BClass>)`.
  Execution halted
Status: 1 ERROR
See
```

## class-add-prop

A adds a property that doesn't clash with B's subclass. Expect everything OK once evoB is rebuilt; the stale stage shows what happens when only evoA is upgraded.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | OK |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | OK |

## class-add-prop-clash

A adds a property whose name B's subclass already uses, with an incompatible type. Expect evoB to fail to (re)install against 2.0.0 because the override doesn't extend the parent property.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | ERROR |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | 1 ERROR |

`evoB install (v2)`:

```
Error in new_class(parent = AFoo, properties = list(y = class_double),  : 
  <Baz>@y must narrow <evoA::Foo>@y.
Error: unable to load R code in package ‘evoB’
Execution halted
ERROR: lazy loading failed for package ‘evoB’
* removing ‘/private<lab>/class-add-prop-clash/lib/evoB’
```

`evoB R CMD check (v2)`:

```
* checking whether package ‘evoB’ can be installed ... ERROR
Installation failed.
Status: 1 ERROR
See
```

## class-remove-prop

A removes a property outright. Expect evoB to still install, but its uses of the property (constructor argument, `@count`) fail at run time, i.e. in tests/examples.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | ERROR |
| evoB install (v2) | OK |
| evoB test (v2) | ERROR |
| evoB R CMD check (v2) | 1 ERROR |

`evoB test (v2, stale)`:

```
Error in evoA::Foo(size = 1, count = 2) : unused argument (count = 2)
Execution halted
```

`evoB test (v2)`:

```
Error in evoA::Foo(size = 1, count = 2) : unused argument (count = 2)
Execution halted
```

`evoB R CMD check (v2)`:

```
 ERROR
Running the tests in ‘tests/smoke.R’ failed.
  Error in evoA::Foo(size = 1, count = 2) : unused argument (count = 2)
  Execution halted
Status: 1 ERROR
See
```

## class-remove-prop-deprecated

A deprecates a property by replacing it with a dynamic property whose getter warns. Expect B's reads of `@count` to keep working against 2.0.0, now with a deprecation warning.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | WARNING |
| evoB install (v2) | OK |
| evoB test (v2) | WARNING |
| evoB R CMD check (v2) | OK |

`evoB test (v2, stale)`:

```
Warning message:
In `<Foo>@count`(<object>) : @count is deprecated; use @size instead
```

`evoB test (v2)`:

```
Warning message:
In `<Foo>@count`(<object>) : @count is deprecated; use @size instead
```

## class-narrow-prop

A narrows a property's type from numeric to integer. Expect evoB to install fine but fail at run time when it constructs an instance with a double.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | ERROR |
| evoB install (v2) | OK |
| evoB test (v2) | ERROR |
| evoB R CMD check (v2) | 1 ERROR |

`evoB test (v2, stale)`:

```
Error in evoA::Foo(size = 1) : <evoA::Foo> object properties are invalid:
- @size must be <integer>, not <double>
```

`evoB test (v2)`:

```
Error in evoA::Foo(size = 1) : <evoA::Foo> object properties are invalid:
- @size must be <integer>, not <double>
```

`evoB R CMD check (v2)`:

```
 ERROR
Running the tests in ‘tests/smoke.R’ failed.
  Error in evoA::Foo(size = 1) : <evoA::Foo> object properties are invalid:
  - @size must be <integer>, not <double>
Status: 1 ERROR
See
```

## class-rename

A renames a class with no alias. Expect evoB to fail to (re)install against 2.0.0: the external class <evoA::Foo> can't be resolved, for either the subclass or the method registration.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | ERROR |
| evoB install (v2) | ERROR |
| evoB test (v2) | ERROR |
| evoB R CMD check (v2) | 1 ERROR |

`evoB test (v2, stale)`:

```
Error : Package 'evoA' must export `Foo` as the S7 class <evoA::Foo>.
Error: 'Foo' is not an exported object from 'namespace:evoA'
Execution halted
```

`evoB install (v2)`:

```
Error : Package 'evoA' must export `Foo` as the S7 class <evoA::Foo>.
Error: unable to load R code in package ‘evoB’
Execution halted
ERROR: lazy loading failed for package ‘evoB’
* removing ‘/private<lab>/class-rename/lib/evoB’
```

`evoB test (v2)`:

```
Error : Package 'evoA' must export `Foo` as the S7 class <evoA::Foo>.
Error: 'Foo' is not an exported object from 'namespace:evoA'
Execution halted
```

`evoB R CMD check (v2)`:

```
* checking whether package ‘evoB’ can be installed ... ERROR
Installation failed.
Status: 1 ERROR
See
```

## class-rename-alias-external

A renames a class but keeps the old name as an exported alias. Expect this NOT to help B when B refers to the class with new_external_class(): resolution checks the class's real name.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | ERROR |
| evoB install (v2) | ERROR |
| evoB test (v2) | ERROR |
| evoB R CMD check (v2) | 1 ERROR |

`evoB test (v2, stale)`:

```
Error in new_object(evoA::Foo(...), y = y) : 
  `_parent` must be an instance of <evoA::Foo>, not <evoA::Bar>.
```

`evoB install (v2)`:

```
Error : Package 'evoA' must export `Foo` as the S7 class <evoA::Foo>.
Error: unable to load R code in package ‘evoB’
Execution halted
ERROR: lazy loading failed for package ‘evoB’
* removing ‘/private<lab>/class-rename-alias-external/lib/evoB’
```

`evoB test (v2)`:

```
Error in new_object(evoA::Foo(...), y = y) : 
  `_parent` must be an instance of <evoA::Foo>, not <evoA::Bar>.
```

`evoB R CMD check (v2)`:

```
* checking whether package ‘evoB’ can be installed ... ERROR
Installation failed.
Status: 1 ERROR
See
```

## class-rename-alias-direct

As class-rename-alias-external, but B uses the class object directly (`parent = evoA::Foo`). Expect a rebuilt B to work, since the alias binding points to a real class (now named evoA::Bar).

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | OK |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | 1 NOTE |

## class-make-abstract

A makes a class abstract. Expect B's subclass to be unaffected (the smoke test only builds the subclass); direct `evoA::Foo()` calls would fail at run time.

| Stage | Result |
|-------|--------|
| evoA 1.0.0: install | OK |
| evoB install (v1) | OK |
| evoB test (v1) | OK |
| evoA 2.0.0: install | OK |
| evoB test (v2, stale) | OK |
| evoB install (v2) | OK |
| evoB test (v2) | OK |
| evoB R CMD check (v2) | OK |

