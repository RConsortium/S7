% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/base-environment.R
\name{class_environment}
\alias{class_environment}
\title{Use an environment as the base type of an S7 class}
\usage{
class_environment
}
\description{
\ifelse{html}{
{\figure{lifecycle-experimental.svg}{options: alt='\[Experimental\]'}}}{\strong{\[Experimental\]
}}

\code{class_environment} is the \link[=base_classes]{base} wrapper for environments.
Unlike all other R objects, environments have reference semantics, i.e., they
are modified in place. It's not clear what all the implications of this are
for S7, so we are marking the use of \code{class_environment} as experimental.

Its use is subject to the following caveats:
\itemize{
\item \code{\link[=S7_data]{S7_data()}} and \verb{S7_data<-()} error, because swapping the underlying data
would destroy the existing attributes.
\item The default \code{\link[=convert]{convert()}} method errors when upcasting to an environment
because stripping the subclass's properties would mutate \code{from} in place.
}
}
\examples{
Counter := new_class(class_environment)
counter <- Counter()
counter$n <- 0L

# Reference semantics: `copy` and `counter` are the same object, so
# mutating one is visible through the other.
copy <- counter
copy$n <- 10L
counter$n
}
