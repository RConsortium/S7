% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bind.R
\name{:=}
\alias{:=}
\title{Create and name an object in one step}
\usage{
lhs := rhs
}
\arguments{
\item{lhs}{A bare symbol: both the name of the variable to create and the
\code{name} supplied to the right-hand side.}

\item{rhs}{A call to a function with a \code{name} argument.}
}
\value{
The result of evaluating \code{rhs}, which is also assigned to \code{lhs} in
the calling environment, returned invisibly.
}
\description{
Functions like \code{\link[=new_class]{new_class()}} and \code{\link[=new_generic]{new_generic()}} take a \code{name} that, by
convention, matches the name of the variable that you assign their result
to. The \verb{:=} operator eliminates this duplication: \code{Foo := new_class()} is
equivalent to \code{Foo <- new_class(name = "Foo")}.

\verb{:=} works with any function that has a \code{name} argument, but bear in mind
that it adds \code{name} to the call as a named argument, so any other arguments
supplied positionally will shift to fill the remaining parameters.
}
\examples{
Range := new_class(properties = list(
  start = class_double,
  end = class_double
))
Range

describe := new_generic("x")
describe
}
